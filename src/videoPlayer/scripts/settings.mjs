// ToDo: verify these match what is actually used in videoPlayer
/** @type {object} videoPlayerSettings */
export const settings = {
    active: false,
    enabled: true,
    buffer:  null,
    command: "",
    currentTime: 0,
    loop: false,
    mimeType: "",
    status: "uninitialized",
    videoTimeOffsetMs: 0
}

// selfViewSettingsPrototype

/** @typedef {Object} selfViewSettings */
export const settings = {
    hideView: {
        enabled: false,
        active: false
    },
    showFraming: {
        enabled: false,
        active: false
    }
}
